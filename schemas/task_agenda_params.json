{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TaskAgendaParams",
  "type": "object",
  "properties": {
    "from": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int64"
    },
    "includeCompleted": {
      "type": "boolean",
      "default": false
    },
    "includePaused": {
      "type": "boolean",
      "default": false
    },
    "limit": {
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0
    },
    "ownerId": {
      "type": [
        "string",
        "null"
      ]
    },
    "ownerKind": {
      "anyOf": [
        {
          "$ref": "#/$defs/TaskOwnerKind"
        },
        {
          "type": "null"
        }
      ]
    },
    "statuses": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/TaskStatus"
      }
    },
    "to": {
      "type": [
        "integer",
        "null"
      ],
      "format": "int64"
    },
    "triggerKinds": {
      "type": "array",
      "default": [],
      "items": {
        "$ref": "#/$defs/TaskTriggerKind"
      }
    },
    "workspaceId": {
      "type": "string"
    }
  },
  "required": [
    "workspaceId"
  ],
  "$defs": {
    "TaskOwnerKind": {
      "type": "string",
      "enum": [
        "user",
        "thread",
        "workspace",
        "system"
      ]
    },
    "TaskStatus": {
      "type": "string",
      "enum": [
        "draft",
        "scheduled",
        "queued",
        "running",
        "waiting",
        "waiting_review",
        "completed",
        "failed",
        "blocked",
        "cancelled"
      ]
    },
    "TaskTriggerKind": {
      "type": "string",
      "enum": [
        "immediate",
        "scheduled_at",
        "interval",
        "cron",
        "manual",
        "external",
        "dependency"
      ]
    }
  }
}