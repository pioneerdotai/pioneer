# Codex MCP adapter fixtures

Recorded schema and lifecycle examples used by local deterministic adapter tests
live one directory above this file. They validate parsing and event mapping
without starting a provider process or requiring account credentials.

Runtime availability remains based on the production technical readiness probe;
these fixtures are stable regression inputs only.
